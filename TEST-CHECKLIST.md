# Crisp File Explorer v0.2.63 测试清单

作者：letschips（小红书博主）

## 自动检查

- [ ] `node --check main.js` 通过。
- [ ] `node --test tests/regression.test.js` 全部通过。
- [ ] `manifest.json` 版本为 `0.2.63`。
- [ ] `assets/` 中不存在未引用的旧素材。
- [ ] `main.js`、`styles.css` 和资源文件不包含本机绝对路径。

## 主窗口回归

- [ ] 文件浏览器正常显示轨道、刻度和当前 orb。
- [ ] 点击不同文档时 orb 使用原有弹簧动画移动，没有先回顶部。
- [ ] 上下拖动保持在竖线中心，不发生绕点公转或横向漂移。
- [ ] 拖动到顶部和底部时边缘滚动正常。
- [ ] 释放拖动后按当前设置打开文件或展开文件夹。
- [ ] 关闭“松开打开项目”后，释放拖动会回到当前活动文件。
- [ ] 折叠活动文件的父文件夹后，orb 定位到可见祖先，不停留在空白区域。
- [ ] orb 定位到文件夹时，文件夹标题右移，不与刻度线重叠。
- [ ] 没有文件打开时，orb 所在文件夹标题仍然右移，不与横线重叠。
- [ ] 连续左右拖动文件栏分界线时，当前选中标题不闪动，横向位移保持稳定。
- [ ] 搜索、书签和其他侧边栏页面能够正常显示。
- [ ] `Cmd+E` 及其他 Obsidian 全局快捷键不被拦截。

## 多窗口回归

- [ ] 将文件浏览器移动到 Obsidian 独立窗口后，轨道和 orb 能正常创建。
- [ ] 独立窗口中的 orb 可以拖动、滚动和释放。
- [ ] 拖动独立窗口中的 orb 不会影响主窗口文件浏览器。
- [ ] 独立窗口释放拖动后能正常打开目标文件或展开目标文件夹。
- [ ] 调整独立窗口大小后，轨道位置会重新测量。
- [ ] 关闭独立窗口后控制器和拖动监听被清理，无控制台错误。
- [ ] 重新打开或移回主窗口后插件能够重新创建控制器。
- [ ] 拖动期间迁移文件浏览器后，原窗口不残留拖动监听器。

## 设置与资源

- [ ] 所有 orb 选项均能加载，图片失败时回退到默认 orb。
- [ ] 静态人物保持正向；旋转类素材围绕自身中心旋转。
- [ ] `Random per day` 在同一天保持稳定。
- [ ] Tick、release 和 Match orb 音效按设置工作。
- [ ] 开启“音高滑动”后，各拖动音效保留所选音色，并随位置升高音高。
- [ ] 修改设置后重载 Obsidian，设置仍然保留。
- [ ] 重命名或删除文件/文件夹后，今日轨迹与常用文件统计不保留旧路径。
- [ ] 文件右键菜单可以固定或取消固定到 Crisp Rail，文件夹菜单不显示该操作。
- [ ] 固定点最多 8 个，固定点使用更实的圆点，智能推荐使用原有淡色圆点。
- [ ] 重命名、移动或删除固定文件及其父文件夹后，固定路径同步更新或清理。
- [ ] 智能推荐优先近期使用文件，超过 90 天未打开的非固定文件退出候选。

## 兼容性与性能

- [ ] 浅色和深色主题下轨道及文字可见。
- [ ] Baseline 与 Border 下活动标题使用相同结构，切换前后文件行高度不变。
- [ ] 小球位于文件或文件夹时只显示横向轨道，不出现活动项竖线。
- [ ] 主题活动背景、阴影和伪元素不会覆盖插件活动反馈。
- [ ] 活动颜色跟随主题运行时强调色，字体、图标和基础行距仍由主题控制。
- [ ] 系统开启“减少动态效果”后不再执行弹簧、旋转和反馈动画。
- [ ] 多个文件浏览器面板各自拥有独立 orb。
- [ ] 500 个以上可见项目时拖动与文件切换无明显掉帧。
- [ ] 开启 Activity / Heatmap 后连续点击不同文档，横线、今日轨迹和常用文件标记不闪白或消失一帧。
- [ ] Obsidian 开发者错误日志没有新增错误。

## 发布检查

- [ ] 待主仓库验收后，再同步其他运行仓库；各仓库分别保留自己的 `data.json`。
- [ ] 各目标仓库均重载到 v0.2.63。
- [ ] 分享 ZIP 仅包含 `main.js`、`styles.css` 和 `manifest.json`；Orb 已内联，不包含源 `assets/`。
- [ ] 分享 ZIP 不包含 `data.json`、测试、说明文档、本机路径或私密配置。
